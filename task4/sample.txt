program ArrayAverage;
var
  grade : char ;
  isready : boolean ;
  a,g : array [1..10] of Integer ;
  number, i, sum : Integer;
  maxValue : real;
  // the programs starts here
begin
  d := 3+5*l;
  if( a[i]>5) then
  begin
    b:=1*2+3-g[4*3+2];
    write(e);
  end
  else
  begin
    c:=2*4/8;
    read(f);
  end;
end.
